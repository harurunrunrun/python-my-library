#!/bin/sh
if [ $# = 1 ]; then
  name=$1
  if [ -f ./input.txt ]; then
    if [ -f ./${name%.*}.py ]; then
      py -3.8 ${name%.*}.py < input.txt
    else
      echo ${name%.*}.py is not exist
    fi
  else
    echo input.txt is not exist
  fi
else
  echo requirements is not one
fi
